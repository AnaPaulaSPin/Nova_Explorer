import { Component } from '@angular/core';
import { PageShellComponent } from '../../shared/page-shell/page-shell.component';

@Component({
  selector: 'app-tags',
  imports: [PageShellComponent],
  template: '<app-page-shell title="Tags" />'
})
export class TagsComponent {}
